# 📦 Руководство по публикации проекта GAS в Git и GitHub (Этап 1)

> **Для новичков в системах контроля версий**. Все действия выполняются вручную в терминале VS Code. Каждый пункт снабжён подробным объяснением, примером команды и ожидаемым результатом. После выполнения шага проверяйте вывод и переходите к следующему.

---

## 📋 Содержание
1. [Инициализация локального репозитория Git](#1-инициализация-локального-репозитория-git)  
2. [Создание `.gitignore`](#2-создание-gitignore)  
3. [Первый коммит](#3-первый-коммит)  
4. [Создание репозитория на GitHub](#4-создание-репозитория-на-github)  
5. [Связывание локального репозитория с удалённым (remote)](#5-связывание-локального-репозитория-с-удалённым-remote)  
6. [Отправка (push) кода в GitHub](#6-отправка-push-кода-в-github)  
7. [Проверка статуса и истории коммитов](#7-проверка-статуса-и-истории-коммитов)  
8. [Настройка ветки `main` и защита ветки (опционально)](#8-настройка-ветки-main-и-защита-ветки-опционально)  
9. [Дополнительные полезные команды](#9-дополнительные-полезные-команды)  

---

## 1️⃣ Инициализация локального репозитория Git
**Зачем?** Git — это система контроля версий, позволяющая отслеживать изменения файлов, откатываться к предыдущим версиям и совместно работать с другими разработчиками.

**Шаги:**
1. Откройте терминал в VS Code (`Ctrl+``) и перейдите в корень проекта:
   ```bash
   cd e:/Обсидиан/GAS/gas-learning-sandbox
   ```
2. Инициализируйте репозиторий:
   ```bash
   git init
   ```
3. Вы увидите сообщение `Initialized empty Git repository in .../.git/`.

**Что происходит?** Создаётся скрытая папка `.git`, где Git хранит всю историю проекта.

---

## 2️⃣ Создание `.gitignore`
**Зачем?** Чтобы исключить из репозитория файлы, которые не нужны в GitHub (например, файлы конфигураций, кеши, `node_modules`).

**Файл `.gitignore` для проекта GAS:**
```text
# Node.js
node_modules/
npm-debug.log

# Google Apps Script
.clasp.json          # хранит ваш scriptId, не публикуем публично
.vscode/            # настройки IDE
.DS_Store           # macOS‑специфичный файл
```

**Как создать:**
```bash
echo "# Node.js\nnode_modules/\nnpm-debug.log\n\n# Google Apps Script\n.clasp.json\n.vscode/\n.DS_Store" > .gitignore
```
Проверьте содержимое:
```bash
cat .gitignore
```

---

## 3️⃣ Первый коммит
**Шаги:**
1. Добавьте все файлы (кроме игнорируемых) в индекс:
   ```bash
   git add .
   ```
2. Проверьте, какие файлы будут закоммичены:
   ```bash
   git status
   ```
   Вы увидите список `new file:   ...`.
3. Сделайте коммит с осмысленным сообщением:
   ```bash
   git commit -m "Initial commit – scaffold GAS project, hello world script"
   ```
4. После коммита вы увидите строку `1 file changed, X insertions(+)` и хеш коммита.

---

## 4️⃣ Создание репозитория на GitHub
1. Откройте <https://github.com> и войдите в свой аккаунт.  
2. Нажмите **New repository**.  
3. Заполните форму:  
   - **Repository name:** `gas-learning-sandbox` (можно добавить `-sandbox` для уникальности).  
   - **Description:** `Google Apps Script learning sandbox`.  
   - **Public/Private:** выбирайте по желанию (для обучения обычно Public).  
   - **Initialize this repository with a README:** **не ставьте** галочку – у вас уже есть файлы.  
4. Нажмите **Create repository**.  
5. После создания GitHub покажет URL репозитория, например:  
   ```
   https://github.com/ваш_логин/gas-learning-sandbox.git
   ```

---

## 5️⃣ Связывание локального репозитория с удалённым (remote)
**Шаги:**
1. Скопируйте URL репозитория (HTTPS‑вариант удобнее для новичков).  
2. В терминале выполните:
   ```bash
   git remote add origin https://github.com/ваш_логин/gas-learning-sandbox.git
   ```
3. Проверьте, что remote добавлен:
   ```bash
   git remote -v
   ```
   Вы увидите две строки с `origin` (fetch и push).

---

## 6️⃣ Отправка (push) кода в GitHub
**Шаги:**
1. Установите ветку `main` как текущую (если Git создал `master`):
   ```bash
   git branch -M main
   ```
2. Отправьте код в удалённый репозиторий:
   ```bash
   git push -u origin main
   ```
3. При первом push Git запросит ваш логин и пароль GitHub.  
   - **Пароль:** используйте **Personal Access Token (PAT)**, а не обычный пароль (GitHub больше не принимает пароли).  
   - Чтобы создать PAT: Settings → Developer settings → Personal access tokens → Generate new token → выберите `repo` → скопируйте токен.  

4. После успешного push в браузере откройте репозиторий – вы увидите все файлы.

---

## 7️⃣ Проверка статуса и истории коммитов
- Текущий статус:
  ```bash
  git status
  ```
  Должно показывать `nothing to commit, working tree clean`.
- История коммитов:
  ```bash
  git log --oneline --graph --decorate
  ```
  Вы увидите ваш первый коммит с пометкой `main`.

---

## 8️⃣ Настройка ветки `main` и защита ветки (опционально)
Для учебных целей можно оставить всё открытым, но если хотите:
1. На GitHub перейдите в **Settings → Branches**.  
2. В разделе **Branch protection rules** нажмите **Add rule**.  
3. В поле **Branch name pattern** введите `main`.  
4. Включите опцию **Require pull request reviews before merging** и **Require status checks** (можно добавить `clasp push` как проверку).  
5. Сохраните правило.

Это предотвратит случайный прямой push в `main` без ревью.

---

## 9️⃣ Дополнительные полезные команды
| Команда | Описание |
|---------|----------|
| `git pull` | Получить изменения из удалённого репозитория. |
| `git branch` | Показать список веток. |
| `git checkout -b feature/xyz` | Создать и переключиться на новую ветку. |
| `git merge feature/xyz` | Слить ветку `feature/xyz` в текущую. |
| `git reset --hard <hash>` | Откатить рабочее дерево к указанному коммиту. |
| `git stash` | Сохранить текущие изменения во временное хранилище. |
| `git stash pop` | Восстановить изменения из stash. |

---

## ✅ Что делать дальше?
1. **Выполните шаг 1‑6** в терминале, следуя инструкциям.  
2. После каждого шага проверяйте вывод и убеждайтесь, что нет ошибок.  
3. Когда код окажется в GitHub, откройте репозиторий в браузере и убедитесь, что все файлы (кроме `.clasp.json` и `.vscode/`) присутствуют.  

Если возникнут вопросы или ошибки – скопируйте полный вывод терминала и пришлите его мне, я помогу разобраться.  

> **Важно:** Не публикуйте файл `.clasp.json` в публичный репозиторий – он содержит ваш `scriptId`. Он уже добавлен в `.gitignore`, поэтому не будет отправлен в GitHub.  

Удачной публикации! 🚀