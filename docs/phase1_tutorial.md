# 📚 Учебник: Этап 1 – Foundation & Environment (10 шагов)

> **Цель** – подготовить полностью рабочее окружение...

---  

## 📋 Сводная таблица всех шагов

| Шаг | Цель                     | Инструкции                                                                       | Команды                                                                                                                                    | Ожидаемый результат                                                      | Типичные ошибки                                                                | Практика                                                       |
| --- | ------------------------ | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------ | -------------------------------------------------------------- |
| 1️⃣ | **Проверка требований**  | Убедиться, что ОС Windows 11, установлен Git (опционально)                       | `git --version`                                                                                                                            | Вывод версии Git (например `git version 2.45.0`)                         | Git не установлен → установить с <https://git-scm.com/download/win>            | Выполнить `git --version` и записать вывод                     |
| 2️⃣ | **Node.js + npm**        | Скачайте и установите LTS‑версию Node.js                                         | `node -v`<br>`npm -v`                                                                                                                      | Вывод версии Node (≥ 16) и npm (≥ 8)                                     | `node`/`npm` не найдены → добавить в PATH или переустановить                   | Установить Node.js, проверить версии                           |
| 3️⃣ | **clasp CLI**            | Установить глобально Google Apps Script CLI                                      | `npm install -g @google/clasp`<br>`clasp -v`                                                                                               | Вывод версии clasp (например `2.4.2`)                                    | `npm` не в PATH → добавить `%APPDATA%\npm` в PATH                              | Установить clasp, проверить версию                             |
| 4️⃣ | **Google auth**          | Авторизовать `clasp` в Google‑аккаунте                                           | `clasp login`                                                                                                                              | Откроется браузер, после входа в консоли появится `Logged in as <email>` | Отклонение доступа → включить “Google Apps Script API” в консоли               | Проверить `clasp whoami`                                       |
| 5️⃣ | **Google Cloud Project** | Создать (или использовать) проект в Google Cloud, получить **Project ID**        | В консоли Cloud → *Create Project* → скопировать ID                                                                                        | ID вида `my-gas-project-123456`                                          | Не включён Billing → включить бесплатный тарифный план                         | Записать ID в файл `project-id.txt`                            |
| 6️⃣ | **Структура VS Code**    | Инициализировать репозиторий, создать папку проекта, добавить файлы конфигурации | `mkdir gas-learning-sandbox && cd gas-learning-sandbox`<br>`clasp create --type standalone --title "GAS Learning Sandbox"`<br>`clasp pull` | Появятся файлы `.clasp.json`, `appsscript.json`, `src/` и др.            | `clasp: command not found` → проверить PATH                                    | Открыть проект в VS Code, убедиться в наличии файлов           |
| 7️⃣ | **Hello World (Sheets)** | Добавить скрипт, создающий меню и выводящий toast                                | В `src/main.gs` добавить `onOpen` и `sayHello`                                                                                             | При открытии таблицы появляется пункт **Sandbox → Say Hello** → toast    | Ошибка синтаксиса → скрипт не загружается, проверьте кавычки и точки с запятой | Изменить сообщение toast и задеплоить                          |
| 8️⃣ | **GAS runtime**          | Понять, как работает среда выполнения (один‑разовый запуск, ограничения)         | Чтение официальной docs: <https://developers.google.com/apps-script/guides/services/quotas>                                                | Понимание лимитов (6 минут, 30 сек CPU)                                  | Превышение лимита → `Exception: Service invoked too many times`                | Написать функцию, которая превышает лимит, увидеть ошибку      |
| 9️⃣ | **Debug & logging**      | Добавить `Logger.log` и просмотреть логи через `clasp`                           | В функции добавить `Logger.log('test')`<br>`clasp logs`                                                                                    | Вывод в терминале строк `[INFO] test`                                    | `clasp logs` пустой → скрипт не запускался                                     | Добавить несколько `Logger.log`, выполнить, посмотреть логи    |
| 🔟  | **Первый проект**        | Скомпилировать всё в один репозиторий, задокументировать                         | `git init`<br>`git add .`<br>`git commit -m "Initial GAS project"`<br>`clasp push`                                                         | Репозиторий готов, файлы синхронизированы                                | Ошибка `git: command not found` → установить Git                               | Создать репозиторий, выполнить `git push` в удалённый (GitHub) |

---  

## 📖 Подробные разделы

### 1️⃣ Проверка требований
**Цель:** Убедиться, что система готова к установке инструментов.  
**Инструкция:**  
1. Откройте PowerShell/Command Prompt.  
2. Выполните `git --version`.  
3. Если команда не найдена – скачайте и установите Git.  

**Команды:**  
```bash
git --version
```

**Результат:**  
```
git version 2.45.0.windows.1
```

**Типичные ошибки:**  
- *`git` не найден* → добавить путь `C:\Program Files\Git\cmd` в переменную `PATH`.  

**Практика:**  
Запишите вывод в файл `requirements.txt`.

### 2️⃣ Node.js + npm
**Цель:** Установить среду выполнения JavaScript.  
**Инструкция:**  
1. Скачайте LTS‑версию с <https://nodejs.org/en/download/> (Windows Installer).  
2. Запустите установщик, оставьте галочку **Add to PATH**.  

**Команды:**  
```bash
node -v
npm -v
```

**Результат:**  
```
v20.12.2
9.8.1
```

**Ошибки:**  
- *`node` не найден* → перезапустите терминал или вручную добавьте `C:\Program Files\nodejs\` в `PATH`.  

**Практика:**  
Создайте файл `hello.js` с `console.log('Hello');` и выполните `node hello.js`.

### 3️⃣ Установка `clasp`
**Цель:** Получить CLI для работы с Apps Script.  
**Инструкция:**  
```bash
npm install -g @google/clasp
clasp -v
```

**Результат:**  
```
2.4.2
```

**Ошибки:**  
- *`npm` не найден* → проверьте установку Node.js.  
- *`clasp` не найден* → добавить `%APPDATA%\npm` в `PATH`.  

**Практика:**  
Выполнить `clasp login` (см. шаг 4).

### 4️⃣ Авторизация `clasp`
**Цель:** Связать CLI с вашим Google‑аккаунтом.  
**Инструкция:**  
```bash
clasp login
```
Откроется браузер, выберите аккаунт, разрешите доступ к **Google Apps Script API**.  

**Результат:**  
```
Logged in as user@example.com
```

**Ошибки:**  
- *Access denied* → включите API в Google Cloud Console (см. шаг 5).  

**Практика:**  
Проверьте статус: `clasp whoami`.

### 5️⃣ Google Cloud Project
**Цель:** Получить Project ID для привязки скриптов.  
**Инструкция:**  
1. Откройте <https://console.cloud.google.com/>.  
2. Создайте новый проект → запомните **Project ID** (например `gas-sandbox-123`).  
3. Включите **Google Apps Script API** (API & Services → Library).  

**Результат:**  
`Project ID: gas-sandbox-123`

**Ошибки:**  
- *Billing not enabled* → включите бесплатный тарифный план.  

**Практика:**  
Запишите ID в файл `project-id.txt`.

### 6️⃣ Структура проекта в VS Code
**Цель:** Инициализировать репозиторий и получить базовые файлы.  
**Инструкция:**  
```bash
mkdir gas-learning-sandbox
cd gas-learning-sandbox
clasp create --type standalone --title "GAS Learning Sandbox"
clasp pull
```

**Результат:**  
- `.clasp.json` (с `scriptId` и `projectId`)  
- `appsscript.json` (манифест)  
- Папка `src/` с `main.gs`  

**Ошибки:**  
- *`clasp: command not found`* → проверьте PATH.  

**Практика:**  
Откройте проект в VS Code, убедитесь, что файлы видны в Explorer.

### 7️⃣ Hello World для Google Sheets
**Цель:** Добавить простую функцию, выводящую toast.  
**Код (`src/main.gs`):**  

```javascript
/**
 * Добавляет пункт меню «Sandbox» в Google Sheets.
 */
function onOpen() {
  SpreadsheetApp.getUi()
    .createMenu('Sandbox')
    .addItem('Say Hello', 'sayHello')
    .addToUi();
}

/**
 * Выводит toast‑сообщение.
 */
function sayHello() {
  SpreadsheetApp.getActiveSpreadsheet()
    .toast('Hello from GAS Learning Sandbox!');
}
```

**Команды:**  
```bash
clasp push
```

**Результат:**  
`Pushed 5 files.`  

**Проверка:** Откройте таблицу, в меню появится **Sandbox → Say Hello** → toast.  

**Ошибки:**  
- *SyntaxError* → скрипт не загружается, проверьте кавычки и точки с запятой.  

**Практика:**  
Измените сообщение toast и повторно задеплойте.

### 8️⃣ Понимание GAS Runtime
**Цель:** Ознакомиться с ограничениями среды.  
**Инструкция:** Прочитать <https://developers.google.com/apps-script/guides/services/quotas>.  

**Ключевые лимиты:**  
- Время выполнения: **6 минут** (скрипт‑триггер) / **30 сек** (веб‑приложения).  
- Память: **256 МБ**.  
- Квоты API (например, 500 запросов/час к `SpreadsheetApp`).  

**Практика:** Написать функцию, которая в бесконечном цикле `Utilities.sleep(1000)`. При запуске получите ошибку `Exception: Service invoked too many times`.  

### 9️⃣ Debug & Logging
**Цель:** Научиться выводить и просматривать логи.  
**Код:**  

```javascript
function debugExample() {
  Logger.log('Start debugExample');
  const sheet = SpreadsheetApp.getActiveSheet();
  Logger.log('Active sheet name: %s', sheet.getName());
  // ... ваш код
  Logger.log('End debugExample');
}
```

**Команды:**  
```bash
clasp push
clasp logs
```

**Результат:**  

```
[INFO] Start debugExample
[INFO] Active sheet name: Sheet1
[INFO] End debugExample
```

**Ошибки:**  
- *No logs* → скрипт не запускался, проверьте триггер.  

**Практика:** Добавьте `Logger.log(JSON.stringify(e))` в `catch`‑блок и проверьте вывод при ошибке.

### 🔟 Первый полноценный проект
**Цель:** Скомпилировать всё в репозиторий и задокументировать.  

**Шаги:**  

```bash
git init
git add .
git commit -m "Initial GAS project"
git remote add origin <your‑github‑repo‑url>
git push -u origin master
clasp push
```

**Результат:**  
- Код в GitHub.  
- Скрипт в облаке, готов к использованию.  

**Ошибки:**  
- *`git` не найден* → установить Git.  
- *`remote origin already exists`* → проверьте URL.  

**Практика:**  
Создайте ветку `feature/menu`, добавьте новый пункт меню, сделайте PR в GitHub, задеплойте.

---  

## ✅ Итоги

| Шаг | Статус |
|-----|--------|
| 1️⃣ | ✅ Проверено |
| 2️⃣ | ✅ Установлен Node.js |
| 3️⃣ | ✅ `clasp` готов |
| 4️⃣ | ✅ Авторизация выполнена |
| 5️⃣ | ✅ Проект в Google Cloud создан |
| 6️⃣ | ✅ Структура проекта готова |
| 7️⃣ | ✅ Hello World работает |
| 8️⃣ | ✅ Понимание лимитов |
| 9️⃣ | ✅ Логи и отладка настроены |
| 🔟 | ✅ Первый проект задокументирован и в Git |

---  

### 📌 Что дальше?

- **Этап 2** – Core Fundamentals (Google Sheets API, батч‑операции, триггеры, кеширование).  
- **Этап 3** – Advanced Patterns & Architecture (MVC, сервис‑слой, DI).  

Если готовы перейти к следующему этапу, дайте знать, и я подготовлю подробный план и практические задания. 🚀