# 📥 Пошаговая инструкция по установке Git для Windows и добавлению в PATH

## 1️⃣ Скачивание установщика
1. Откройте браузер и перейдите по ссылке:  
   <https://git-scm.com/download/win>
2. На открывшейся странице автоматически начнётся загрузка файла **`Git-2.xx.x-64-bit.exe`** (где `2.xx.x` – текущая версия).  
3. Дождитесь завершения загрузки.

## 2️⃣ Запуск установщика
1. Дважды щёлкните по загруженному файлу `Git-2.xx.x-64-bit.exe`.  
2. Появится мастер установки. Нажимайте **Next**, пока не дойдёте до экрана **Select Components**.

## 3️⃣ Выбор компонентов (рекомендовано)
- **Git Bash Here** – позволяет открывать терминал Git из контекстного меню Проводника.  
- **Git GUI Here** – графический клиент (по желанию).  
- **Associate .git* configuration files with the default text editor** – удобно, но необязательно.  
- **Enable symbolic links** – оставьте по умолчанию.  

Нажмите **Next**.

## 4️⃣ Выбор редактора (по умолчанию VS Code)
- На экране **Choosing the default editor used by Git** выберите **Visual Studio Code** (если установлен) или **Use Vim** / **Notepad++** – любой удобный вам редактор.  
- Нажмите **Next**.

## 5️⃣ Настройка пути (важный шаг)
- На экране **Adjusting your PATH environment** выберите **“Git from the command line and also from 3rd‑party software”**.  
  Это добавит каталог `C:\Program Files\Git\cmd` в системную переменную **PATH**, и команда `git` будет доступна в любой консоли (PowerShell, CMD, терминал VS Code).  
- Нажмите **Next**.

## 6️⃣ Выбор HTTPS‑транспорта
- Оставьте **“Use the OpenSSL library”** (по умолчанию).  
- Нажмите **Next**.

## 7️⃣ Выбор поведения line‑ending
- Выберите **“Checkout Windows‑style, commit Unix‑style line endings”** – стандарт для кроссплатформенных проектов.  
- Нажмите **Next**.

## 8️⃣ Конфигурация кеша учетных данных
- Оставьте **“Git Credential Manager Core”** – упрощает ввод логина/пароля при работе с удалёнными репозиториями.  
- Нажмите **Next**.

## 9️⃣ Выбор опций терминала
- На экране **“Configuring the terminal emulator to use with Git Bash”** оставьте **“Use MinTTY (the default terminal of MSYS2)”**.  
- Нажмите **Next**.

## 🔟 Завершение установки
- На последнем экране нажмите **Install**.  
- Дождитесь завершения копирования файлов.  
- После установки нажмите **Finish**. При желании поставьте галочку **“Launch Git Bash”** для мгновенного открытия терминала.

## 11️⃣ Проверка установки
1. Откройте **PowerShell** или **Command Prompt** (или терминал VS Code).  
2. Выполните команду:
   ```bash
   git --version
   ```
3. Ожидаемый вывод, например:
   ```
   git version 2.45.0.windows.1
   ```
   Если вывод появился – Git успешно установлен и находится в PATH.

## 12️⃣ Что делать, если `git` всё ещё не найден
- Перезапустите VS Code (он читает переменные окружения только при запуске).  
- Убедитесь, что путь `C:\Program Files\Git\cmd` действительно присутствует в переменной **PATH**:
  1. Откройте **Панель управления → Система → Дополнительные параметры системы → Переменные среды**.  
  2. В разделе **Системные переменные** найдите **Path**, нажмите **Изменить** и проверьте наличие строки `C:\Program Files\Git\cmd`.  
  3. При необходимости добавьте её и сохраните изменения.  
- После изменения PATH снова откройте терминал и выполните `git --version`.

## 📌 Итоги
- Вы скачали и установили Git.  
- Добавили Git в системный PATH, что делает команду `git` доступной в любой консоли.  
- Проверили работоспособность команды `git --version`.  

Теперь вы готовы к дальнейшим шагам: инициализировать репозиторий, создать `.gitignore`, сделать первый коммит и опубликовать проект на GitHub (см. файл `git_publish_guide.md` в той же папке).

---  

*Если возникнут вопросы на каком‑либо этапе, скопируйте точный вывод терминала и пришлите его – я помогу разобраться.* 